version: "3"

services:

  elobot:
    image: sogemoge/tf-1726:latest
    restart: always
    ports:
      - 0.0.0.0:8080:800
    volumes:
      - type: bind
        source: ./elo.db
        target: /opt/TF-1726/elo.db
      - type: bind
        source: ./.env
        target: /opt/TF-1726/.env
#    healthcheck:
#      test: ["CMD", "curl", "127.0.0.1"]
#      interval: 10s
#      timeout: 5s
#      retries: 5
